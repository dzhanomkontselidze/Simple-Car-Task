Solution for the task:

"Using an object from the repository, write the functions:

1)getShortDescription(car) which returns a string with a short description of brand, title, color;

2)getYearAsNumber() which returns the year in numeric format;

3)setDescription(car, description) which adds a field with a short description to the object;

4)getPriceByExchangeRate(rate) which returns the price at a given exchange rate;

(consider the possibility of the absence of a property in the object)"
